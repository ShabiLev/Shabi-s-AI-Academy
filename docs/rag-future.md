# Future RAG boundary

RAG is not active in the beta. A future implementation would require versioned chunk metadata, source spans, an embedding model identifier, a vector store, retrieval ranking, citations, deletion propagation, evaluation, and privacy controls.

The current Knowledge Base performs deterministic local text matching only and never labels matches as model-grounded answers.
