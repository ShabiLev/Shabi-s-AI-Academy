import { test, expect, login } from "../fixtures/academy";
import { writeQualityArtifact } from "../fixtures/qualityArtifacts";
const routes = ["/", "/dashboard", "/lessons", "/prompts", "/agents", "/workflows", "/help", "/settings"];
test("primary screens contain no raw keys or misleading live-AI claims", async ({ page }) => { await login(page); const findings: Array<{ route: string; severity: string; finding: string }> = []; for (const route of routes) { await page.goto(route); const text = await page.locator("body").innerText(); if (/\b(?:translation|i18n)\.[a-z][\w.]+\b/i.test(text)) findings.push({ route, severity: "High", finding: "Raw translation key" }); if (/AI generated/i.test(text) && !/mock|simulat/i.test(text)) findings.push({ route, severity: "Medium", finding: "AI-generated wording lacks simulation context" }); } writeQualityArtifact("copy-quality", { status: findings.length ? "warning" : "passed", auditedRoutes: routes.length, findings }); expect(findings.filter((item) => item.severity === "High")).toEqual([]); });
