import { useMemo } from "react";
import { useLanguage } from "../i18n/LanguageContext";
import { measureWorkspaceStorage, useWorkspace } from "../workspace";
export function AnalyticsPage() { const { language } = useLanguage(); const { state, setAnalyticsEnabled, reset } = useWorkspace(); const storage = measureWorkspaceStorage(); const counts = useMemo(() => Object.entries(state.analytics.reduce<Record<string,number>>((result, event) => ({ ...result, [event.type]: (result[event.type] ?? 0) + 1 }), {})).sort((left, right) => right[1] - left[1]), [state.analytics]); const download = () => { const blob = new Blob([JSON.stringify({ schemaVersion: 1, exportedAt: new Date().toISOString(), events: state.analytics }, null, 2)], { type: "application/json" }); const url = URL.createObjectURL(blob); const link = document.createElement("a"); link.href = url; link.download = "academy-local-analytics.json"; link.click(); URL.revokeObjectURL(url); }; return <div className="page analytics-page"><header className="page-heading"><div><h1>{language === "he" ? "ניתוח שימוש" : "Usage analytics"}</h1><p>{language === "he" ? "ניתוח מקומי בדפדפן — ללא תוכן פרומפטים, מסמכים, מידע אישי או סודות." : "Browser-local analytics — no prompt content, document bodies, personal data, or secrets."}</p></div></header><section className="analytics-controls"><label><input type="checkbox" checked={state.analyticsEnabled} onChange={(event) => setAnalyticsEnabled(event.target.checked)} />{language === "he" ? "איסוף אירועים מקומיים" : "Collect local events"}</label><button type="button" onClick={download}>{language === "he" ? "ייצוא ניתוח" : "Export analytics"}</button><button type="button" onClick={() => reset("analytics")}>{language === "he" ? "ניקוי ניתוח" : "Clear analytics"}</button></section><section><h2>{language === "he" ? "מדדי סביבת העבודה" : "Workspace metrics"}</h2><div className="analytics-metrics"><article><strong>{state.analytics.length}</strong><span>{language === "he" ? "אירועים בטוחים" : "safe events"}</span></article><article><strong>{state.activities.length}</strong><span>{language === "he" ? "פעילויות אחרונות" : "recent activities"}</span></article><article><strong>{state.preferences.filter((item) => item.favorite).length}</strong><span>{language === "he" ? "מועדפים" : "favorites"}</span></article><article><strong>{Math.round(storage.totalBytes / 1024)} KB</strong><span>{language === "he" ? "אחסון אקדמיה" : "Academy storage"}</span></article></div></section><section><h2>{language === "he" ? "אירועים לפי סוג" : "Events by type"}</h2>{counts.length ? <table><thead><tr><th>{language === "he" ? "אירוע" : "Event"}</th><th>{language === "he" ? "כמות" : "Count"}</th></tr></thead><tbody>{counts.map(([type, count]) => <tr key={type}><td>{type}</td><td>{count}</td></tr>)}</tbody></table> : <p>{language === "he" ? "אין עדיין אירועים." : "No events yet."}</p>}</section><section><h2>{language === "he" ? "שימוש באחסון" : "Storage usage"}</h2>{storage.nearCapacity && <p role="alert">{language === "he" ? "האחסון המקומי מתקרב לקיבולת. לא נמחק מידע אוטומטית." : "Local storage is nearing capacity. No data is deleted automatically."}</p>}<table><thead><tr><th>{language === "he" ? "תחום" : "Domain"}</th><th>{language === "he" ? "בתים" : "Bytes"}</th></tr></thead><tbody>{storage.domains.map((domain) => <tr key={domain.key}><td>{domain.key}</td><td>{domain.bytes}</td></tr>)}</tbody></table></section></div>; }

