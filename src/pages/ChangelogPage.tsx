import { useLanguage } from "../i18n/LanguageContext";
const releases = [{ version: "1.0.0-beta.1", items: ["Complete bilingual curriculum", "Prompt packs and starter agents", "Playgrounds, Projects, and Knowledge Base", "Platform and deployment hardening"] }, { version: "0.7.0-alpha.1", items: ["Deterministic Runtime Engine", "Mock and Dry Run", "Approval, retry, cancellation, and Run History"] }, { version: "0.6.1", items: ["Engineering quality platform", "Prompt and Agent libraries"] }];
export function ChangelogPage() { const { language } = useLanguage(); return <div className="page"><h1>{language === "he" ? "יומן שינויים" : "Changelog"}</h1>{releases.map((release) => <article className="settings-card" key={release.version}><h2>{release.version}</h2><ul>{release.items.map((item) => <li key={item}>{item}</li>)}</ul></article>)}</div>; }
