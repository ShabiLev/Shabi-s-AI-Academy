# Agent library

My Agents is the browser-local user-owned library. Starter Agents are read-only built-ins and are not included in My Agents counts until explicitly imported.

Imports preserve a source-template ID, start at version 1, and never overwrite existing copies. Users may open an existing copy or explicitly import another. Mock simulation remains local; planned tools do not execute.
