import { useState, type FormEvent } from "react";
import { Link } from "react-router-dom";
import { loadLocalProfile, saveLocalProfile } from "../account";
import { useAuth } from "../auth";
import { useExperience } from "../experience";
import { useLanguage } from "../i18n/LanguageContext";
import { useOnboarding } from "../onboarding";
import type { Interest } from "../onboarding/types";
const interests: Interest[] = ["qa","sql","product","development","promptEngineering","agents","automation","research"];
export function ProfilePage() {
  const { language, setLanguage } = useLanguage(); const { user, isCloudAuthenticated } = useAuth(); const { mode, developerModeEnabled, setMode } = useExperience(); const { profile: onboarding, save: saveOnboarding, restart } = useOnboarding();
  const [profile, setProfile] = useState(loadLocalProfile); const [saved, setSaved] = useState(false);
  const submit = (event: FormEvent) => { event.preventDefault(); setProfile(saveLocalProfile(profile)); setSaved(true); };
  const toggleInterest = (interest: Interest) => saveOnboarding({ ...onboarding, interests: onboarding.interests.includes(interest) ? onboarding.interests.filter((item) => item !== interest) : [...onboarding.interests, interest], updatedAt: new Date().toISOString() });
  return <div className="page profile-page"><form className="panel profile-form" onSubmit={submit}><h2>{language === "he" ? "פרטים אישיים" : "Personal details"}</h2><div className="auth-name-grid"><label>{language === "he" ? "שם פרטי" : "First name"}<input autoComplete="given-name" value={profile.firstName} placeholder={language === "he" ? user?.displayNameHe : user?.displayNameEn} onChange={(e) => setProfile((value) => ({ ...value, firstName: e.target.value }))} /></label><label>{language === "he" ? "שם משפחה" : "Last name"}<input autoComplete="family-name" value={profile.lastName} onChange={(e) => setProfile((value) => ({ ...value, lastName: e.target.value }))} /></label></div><label>{language === "he" ? "מטרות למידה" : "Learning goals"}<textarea maxLength={500} value={profile.learningGoals} onChange={(e) => setProfile((value) => ({ ...value, learningGoals: e.target.value }))} /></label><button className="button button-primary" type="submit">{language === "he" ? "שמירה מקומית" : "Save locally"}</button>{saved && <p role="status">{language === "he" ? "הפרופיל המקומי נשמר." : "Local profile saved."}</p>}</form><section className="panel"><h2>{language === "he" ? "העדפות חוויה" : "Experience preferences"}</h2><label>{language === "he" ? "שפה מועדפת" : "Preferred language"}<select value={language} onChange={(e) => setLanguage(e.target.value as "he"|"en")}><option value="he">עברית</option><option value="en">English</option></select></label><fieldset><legend>{language === "he" ? "מצב חוויה" : "Experience mode"}</legend><label><input type="radio" checked={mode === "beginner"} onChange={() => setMode("beginner")} />Beginner</label><label><input type="radio" checked={mode === "advanced"} onChange={() => setMode("advanced")} />Advanced</label>{developerModeEnabled && <p>Developer Mode enabled</p>}</fieldset><fieldset><legend>{language === "he" ? "תחומי עניין" : "Interests"}</legend><div className="profile-interests">{interests.map((interest) => <label key={interest}><input type="checkbox" checked={onboarding.interests.includes(interest)} onChange={() => toggleInterest(interest)} />{interest}</label>)}</div></fieldset><button type="button" className="text-button" onClick={restart}>{language === "he" ? "התחלת ההדרכה מחדש" : "Restart onboarding"}</button></section><section className="panel"><h2>{language === "he" ? "חשבון ונתונים" : "Account and data"}</h2><dl className="account-facts"><div><dt>{language === "he" ? "מצב" : "Status"}</dt><dd>{isCloudAuthenticated ? (language === "he" ? "חשבון מחובר; סנכרון טרם אושר" : "Account connected; sync not yet confirmed") : (language === "he" ? "אורח · מקומי בלבד" : "Guest · local only")}</dd></div><div><dt>{language === "he" ? "נוצר מקומית" : "Created locally"}</dt><dd>{new Date(profile.createdAt).toLocaleDateString(language)}</dd></div></dl>{isCloudAuthenticated && <Link to="/account/security">{language === "he" ? "אבטחת חשבון" : "Account security"}</Link>}<div className="inline-actions"><Link to="/settings">{language === "he" ? "גיבוי וניהול נתונים" : "Backup and data management"}</Link><Link to="/onboarding">{language === "he" ? "הגדרות התחלה" : "Onboarding settings"}</Link></div></section></div>;
}
