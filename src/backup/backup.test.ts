import { describe, expect, it } from "vitest";
import { applyWorkspaceImport, backupDomainKeys, checksumPayload, createWorkspaceBackup, previewWorkspaceImport, serializeWorkspaceBackup } from ".";
function memoryStorage(seed: Record<string,string> = {}, failKey = "") { const map = new Map(Object.entries(seed)); return { getItem: (key: string) => map.get(key) ?? null, setItem: (key: string, value: string) => { if (key === failKey) throw new Error("quota"); map.set(key, value); }, removeItem: (key: string) => { map.delete(key); }, map }; }
describe("complete Workspace backup", () => {
  it("exports whitelisted parsed domains with metadata and checksum", () => { const storage = memoryStorage({ [backupDomainKeys.prompts]: JSON.stringify({ schemaVersion: 1, prompts: [{ id: "p1" }] }), [backupDomainKeys.settings]: "en", unrelated: "secret" }); const backup = createWorkspaceBackup(storage, () => "now"); expect(backup.domains.prompts).toBeDefined(); expect(backup.domains.settings).toBe("en"); expect(JSON.stringify(backup)).not.toContain("unrelated"); expect(backup.checksum).toBe(checksumPayload({ schemaVersion: 1, appVersion: "1.9.0-beta.1", exportedAt: "now", domainVersions: backup.domainVersions, domains: backup.domains })); });
  it("previews conflicts and applies merge or replace per domain", () => { const storage = memoryStorage({ [backupDomainKeys.prompts]: JSON.stringify({ schemaVersion: 1, prompts: [{ id: "p1", title: "old" }] }) }); const backup = createWorkspaceBackup(memoryStorage({ [backupDomainKeys.prompts]: JSON.stringify({ schemaVersion: 1, prompts: [{ id: "p1", title: "new" }, { id: "p2" }] }) })); const preview = previewWorkspaceImport(serializeWorkspaceBackup(backup), storage); expect(preview.valid).toBe(true); expect(preview.domains[0].conflicts).toBe(1); const report = applyWorkspaceImport(preview, { prompts: "merge" }, storage); expect(report.ok).toBe(true); expect(JSON.parse(storage.getItem(backupDomainKeys.prompts)!).prompts).toHaveLength(2); });
  it("rejects malformed, oversized, secret-bearing, and modified imports", () => { expect(previewWorkspaceImport("{").errors).toContain("malformed-json"); expect(previewWorkspaceImport("x".repeat(8_000_001)).errors).toContain("oversized-import"); const backup = createWorkspaceBackup(memoryStorage()); const secret = { ...backup, domains: { workspace: { apiKey: "no" } } }; secret.checksum = checksumPayload({ schemaVersion: secret.schemaVersion, appVersion: secret.appVersion, exportedAt: secret.exportedAt, domainVersions: secret.domainVersions, domains: secret.domains }); expect(previewWorkspaceImport(JSON.stringify(secret)).errors).toContain("secret-shaped-key"); const changed = { ...backup, appVersion: "changed" }; expect(previewWorkspaceImport(JSON.stringify(changed)).errors).toContain("checksum-mismatch"); });
  it("rolls back every touched key after a write failure", () => { const source = memoryStorage({ [backupDomainKeys.prompts]: JSON.stringify({ prompts: [{ id: "new" }] }), [backupDomainKeys.agents]: JSON.stringify({ agents: [{ id: "a" }] }) }); const preview = previewWorkspaceImport(serializeWorkspaceBackup(createWorkspaceBackup(source))); const target = memoryStorage({ [backupDomainKeys.prompts]: "old" }, backupDomainKeys.agents); const report = applyWorkspaceImport(preview, { prompts: "replace", agents: "replace" }, target); expect(report.rolledBack).toBe(true); expect(target.getItem(backupDomainKeys.prompts)).toBe("old"); });
  it("includes Version 1.8 and seven actor-scoped Version 1.9 domains", () => { const actor = "actor-1"; const storage = memoryStorage({ "shabis-ai-academy:mission-actor:v1": actor, [`shabis-ai-academy:missions:v1:${actor}`]: JSON.stringify({ schemaVersion: 1, missions: [] }), [`shabis-ai-academy:agent-teams:v1:${actor}`]: JSON.stringify({ schemaVersion: 1, teams: [] }), [`shabis-ai-academy:skill-map:v1:${actor}`]: JSON.stringify({ schemaVersion: 1, progress: [] }), [`shabis-ai-academy:context-packs:v1:${actor}`]: JSON.stringify({ schemaVersion: 1, packs: [] }), [`shabis-ai-academy:mission-analytics:v1:${actor}`]: JSON.stringify({ schemaVersion: 1, events: [] }), [`shabis-ai-academy:evaluation-rubrics:v1:${actor}`]: JSON.stringify({ schemaVersion: 1, rubrics: [] }), [`shabis-ai-academy:evaluation-experiments:v1:${actor}`]: JSON.stringify({ schemaVersion: 1, experiments: [] }), [`shabis-ai-academy:evaluation-runs:v1:${actor}`]: JSON.stringify({ schemaVersion: 1, runs: [] }), [`shabis-ai-academy:evaluation-suites:v1:${actor}`]: JSON.stringify({ schemaVersion: 1, suites: [] }), [`shabis-ai-academy:failure-library:v1:${actor}`]: JSON.stringify({ schemaVersion: 1, cases: [] }), [`shabis-ai-academy:entity-versions:v1:${actor}`]: JSON.stringify({ schemaVersion: 1, versions: [] }), [`shabis-ai-academy:connected-previews:v1:${actor}`]: JSON.stringify({ schemaVersion: 1, previews: [] }) }); const backup = createWorkspaceBackup(storage); expect(Object.keys(backup.domains)).toEqual(expect.arrayContaining(["missions", "agentTeams", "skillMap", "contextPacks", "missionAnalytics", "evaluationRubrics", "evaluationExperiments", "evaluationRuns", "evaluationSuites", "failureLibrary", "entityVersions", "connectedPreviews"])); expect(backup.appVersion).toBe("1.9.0-beta.1"); });
  it("revalidates a preview at apply time and preserves immutable collisions", () => { const source = memoryStorage({ [backupDomainKeys.evaluationRuns]: JSON.stringify({ schemaVersion: 1, runs: [{ id: "run-1", result: "incoming" }, { id: "run-2" }] }) }); const target = memoryStorage({ [backupDomainKeys.evaluationRuns]: JSON.stringify({ schemaVersion: 1, runs: [{ id: "run-1", result: "existing" }] }) }); const preview = previewWorkspaceImport(serializeWorkspaceBackup(createWorkspaceBackup(source)), target); expect(applyWorkspaceImport(preview, { evaluationRuns: "replace" }, target).ok).toBe(true); expect(JSON.parse(target.getItem(backupDomainKeys.evaluationRuns)!).runs).toEqual(expect.arrayContaining([{ id: "run-1", result: "existing" }, { id: "run-2" }])); preview.backup!.domains.evaluationRuns = { schemaVersion: 1, runs: [{ id: "tampered" }] }; expect(applyWorkspaceImport(preview, {}, target).errors).toContain("checksum-mismatch"); });
});
