# Knowledge import

Supported extensions are `.txt`, `.md`, `.markdown`, `.json`, and `.csv`. Filenames are sanitized, JSON must parse, content must be UTF-8 browser text, and documents over 1 MB are rejected. HTML-like input remains visible text and is never executed.

Do not import confidential, personal, credential, or production-sensitive material. Clearing browser storage removes local documents.
