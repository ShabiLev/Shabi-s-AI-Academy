# Assistant Sidebar

The protected application shell includes a Local Assistant in collapsed, compact, and expanded modes. It explains the current route and links to relevant local search, chat, and Help surfaces. Recent Assistant activity appears only in expanded mode.

The sidebar is deterministic and always states that no external AI provider is active. Desktop layouts reserve space; mobile uses an overlay panel that closes with Escape or browser Back and does not create horizontal overflow.

