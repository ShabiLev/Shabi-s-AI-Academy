import { roadmapItems } from "../platform/roadmap"; import { useLanguage } from "../i18n/LanguageContext";
export function RoadmapPage() { const { language } = useLanguage(); return <div className="page"><h1>{language === "he" ? "מפת דרכים" : "Roadmap"}</h1><p>{language === "he" ? "מצב מתועד ולא הבטחת תאריך." : "Documented status, not a date commitment."}</p><ol className="runtime-timeline">{roadmapItems.map((item, index) => <li key={`${item.version}-${index}`}><span>{index + 1}</span><div><h2>{item.version} · {item.title[language]}</h2><p><strong>{item.status}</strong> — {item.summary[language]}</p></div></li>)}</ol></div>; }
