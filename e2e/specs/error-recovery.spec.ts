import { test, expect } from "../fixtures/academy";
test("malformed local data recovers without a blank page or secret disclosure", async ({ page }) => { await page.goto("/login"); await page.evaluate(() => { localStorage.setItem("shabi-ai-academy.prompt-library.v1", "{secret-token-value"); localStorage.setItem("shabis-ai-academy.projects.v1", "{"); }); await page.goto("/prompts"); await page.getByRole("button", { name: /Demo Login|כניסה למצב הדגמה/ }).click(); await expect(page.locator("main")).toBeVisible(); await expect(page.locator("body")).not.toContainText("secret-token-value"); });
test("unknown route never renders a blank application", async ({ page }) => { await page.goto("/quality-route-that-does-not-exist"); await expect(page.locator("body")).not.toBeEmpty(); });
