import { Link, useSearchParams } from "react-router-dom";
import { AuthCard } from "../../components/auth/AuthCard";
import { useLanguage } from "../../i18n/LanguageContext";
export function VerifyEmailPage() { const { language } = useLanguage(); const [params] = useSearchParams(); const email = params.get("email"); return <AuthCard title={language === "he" ? "אימות דוא״ל" : "Verify your email"} description={language === "he" ? "פתחו את הודעת האימות כדי להשלים את יצירת החשבון." : "Open the verification message to finish creating your account."}><p>{email ? (language === "he" ? `ההודעה נשלחה אל ${email}.` : `The message was sent to ${email}.`) : (language === "he" ? "בדקו את תיבת הדוא״ל ואת תיקיית הספאם." : "Check your inbox and spam folder.")}</p><p>{language === "he" ? "קישורים פגי תוקף או שכבר נוצלו יובילו למסך שגיאה בטוח." : "Expired or already-used links lead to a safe error screen."}</p><Link className="button button-primary" to="/auth/login">{language === "he" ? "לכניסה" : "Go to sign in"}</Link></AuthCard>; }
