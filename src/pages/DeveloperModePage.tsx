import { useLanguage } from "../i18n/LanguageContext"; import { appMetadata } from "../config/appMetadata";
const storageKeys = ["shabi-ai-academy.course-progress.v1", "shabi-ai-academy.prompt-library.v1", "shabi-ai-academy.agent-library.v1", "shabis-ai-academy.runtime.runs.v1", "shabis-ai-academy.projects.v1", "shabis-ai-academy.knowledge.v1"];
export function DeveloperModePage() { const { language } = useLanguage(); const he = language === "he"; return <div className="page"><h1>{he ? "מצב מפתחים" : "Developer Mode"}</h1><p>{he ? "כלי בדיקה מקומיים ללא סודות, מפתחות או תוכן מלא." : "Local inspection tools without secrets, keys, or full stored content."}</p><section><h2>{he ? "נתיבים ודגלים" : "Routes and feature flags"}</h2><p>Mock: enabled · Dry Run: enabled · Live Provider: disabled · MCP: disabled</p><p>Version: {appMetadata.version}</p></section><section><h2>{he ? "מלאי אחסון" : "Storage inventory"}</h2><table><thead><tr><th>Key</th><th>Status</th><th>Bytes</th></tr></thead><tbody>{storageKeys.map((key) => { const value = localStorage.getItem(key); return <tr key={key}><td><code>{key}</code></td><td>{value ? "present" : "empty"}</td><td>{value?.length ?? 0}</td></tr>; })}</tbody></table></section><section><h2>{he ? "בטיחות" : "Safety"}</h2><p>{he ? "הצופה אינו מציג ערכי אחסון, תוכן מסמכים או קלטי משתמש." : "The viewer does not display storage values, document bodies, or user inputs."}</p></section></div>; }
