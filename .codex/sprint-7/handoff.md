# Sprint 7 Implementation Handoff

Work in the existing Shabi's AI Academy repository. Read `AGENTS.md`, `.codex/README.md`, the linked architecture and standards, `.codex/sprint-7/00-master-spec.md`, and every referenced Sprint 7 file.

Inspect the current clean application and Git baseline. Preserve unrelated/user work. Create a phased implementation plan, then implement Sprint 7 directly in the specified delivery order. Run focused tests after every phase; update translations, accessibility, responsive behavior, QA Center, How To, architecture, tests, and documentation with the code.

Keep Mock and Dry Run deterministic, Live disabled, tools unconnected, secrets out of browser storage, built-in catalogs separate from personal data, and external text inert. Do not add out-of-scope integrations or invent missing requirements.

At completion, update the application to 0.7.0, run every command in `08-release.md` including `npm run validate:release`, review staged scope/artifacts/secrets, and create exactly:

`feat(runtime): add Agent runtime, Starter Agents and Prompt Packs`

Do not amend history or push. Report exact results, commit SHA, assumptions, limitations, manual checks, and `git push origin main`.
