import { workflowNodeTypes, type Workflow, type WorkflowState } from "./types";
export const WORKFLOW_STORAGE_KEY = "shabis-ai-academy:workflows:v1"; const MAX_WORKFLOWS = 100; const MAX_HISTORY = 100;
const empty = (): WorkflowState => ({ schemaVersion: 1, appVersion: "1.2.0-beta.1", workflows: [], history: [] });
function validWorkflow(value: unknown): value is Workflow { if (!value || typeof value !== "object") return false; const record = value as Workflow; return typeof record.id === "string" && record.name && typeof record.name.he === "string" && Array.isArray(record.nodes) && record.nodes.length <= 50 && record.nodes.every((node) => typeof node.id === "string" && workflowNodeTypes.includes(node.type) && Array.isArray(node.nextNodeIds)); }
export function parseWorkflowState(value: unknown): WorkflowState { if (!value || typeof value !== "object") return empty(); const record = value as Partial<WorkflowState>; const seen = new Set<string>(); return { schemaVersion: 1, appVersion: typeof record.appVersion === "string" ? record.appVersion : "1.2.0-beta.1", workflows: Array.isArray(record.workflows) ? record.workflows.filter(validWorkflow).filter((workflow) => !seen.has(workflow.id) && Boolean(seen.add(workflow.id))).slice(0, MAX_WORKFLOWS) : [], history: Array.isArray(record.history) ? record.history.filter((run) => run && typeof run.id === "string" && Array.isArray(run.events)).slice(-MAX_HISTORY) : [] }; }
export function loadWorkflowState(storage: Pick<Storage,"getItem"> = localStorage) { try { return parseWorkflowState(JSON.parse(storage.getItem(WORKFLOW_STORAGE_KEY) ?? "null")); } catch { return empty(); } }
export function saveWorkflowState(value: WorkflowState, storage: Pick<Storage,"setItem"> = localStorage) { try { storage.setItem(WORKFLOW_STORAGE_KEY, JSON.stringify(parseWorkflowState(value))); } catch { /* optional */ } }

