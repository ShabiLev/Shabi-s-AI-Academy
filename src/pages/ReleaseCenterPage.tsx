import { Link } from "react-router-dom"; import { appMetadata } from "../config/appMetadata"; import { buildMetadata } from "../quality/buildMetadata"; import { useLanguage } from "../i18n/LanguageContext";
export function ReleaseCenterPage() { const { language } = useLanguage(); const he = language === "he"; const gates = ["TypeScript and lint", "Vitest and coverage", "Playwright browser matrix", "Accessibility", "Visual regression", "Lighthouse", "Documentation and catalog validation"]; return <div className="page"><header className="page-header"><div><h1>{he ? "מרכז שחרור" : "Release Center"}</h1><p>{he ? "מטא־נתונים, שערי איכות, בדיקות ידניות והנחיות נסיגה." : "Metadata, quality gates, manual checks, and rollback guidance."}</p></div><Link to="/changelog">Changelog</Link></header><dl className="runtime-facts"><div><dt>Version</dt><dd>{appMetadata.version}</dd></div><div><dt>Commit</dt><dd>{buildMetadata.commitSha}</dd></div><div><dt>Branch</dt><dd>{buildMetadata.branch}</dd></div><div><dt>Build</dt><dd>{buildMetadata.buildTimestamp}</dd></div><div><dt>Production</dt><dd>https://shabi-s-ai-academy.vercel.app</dd></div></dl><section><h2>{he ? "שערי איכות נדרשים" : "Required quality gates"}</h2><ul>{gates.map((gate) => <li key={gate}>{gate} — {he ? "נבדק בדוח האיכות האחרון" : "see latest quality report"}</li>)}</ul><p>{he ? "רשימת הבדיקות הידנית אינה מסומנת אוטומטית כעוברת." : "The manual checklist is never marked passed automatically."}</p></section><section><h2>{he ? "נסיגה" : "Rollback"}</h2><p>{he ? "יש לקדם מחדש פריסת Vercel קודמת במצב READY או לבטל את commit השחרור, להריץ את כל השערים ולפרוס מחדש." : "Promote a previous READY Vercel deployment or revert the release commit, rerun every gate, and redeploy."}</p></section></div>; }
