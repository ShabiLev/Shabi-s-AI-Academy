import { describe, expect, it } from "vitest";
import { measureWorkspaceStorage, parseWorkspaceState, resetWorkspaceDomain, WORKSPACE_MAX_BYTES } from ".";
describe("workspace activity and analytics", () => { it("bounds and validates all local collections", () => { const parsed = parseWorkspaceState({ activities: Array.from({ length: 250 }, (_, index) => ({ id: String(index), entityId: "x", entityType: "prompt", kind: "opened", title: "safe", route: "/prompts", timestamp: "now" })), analytics: [{ id: "bad", type: "routeViewed", timestamp: "now", promptContent: "secret" }] }); expect(parsed.activities).toHaveLength(200); expect(parsed.analytics).toEqual([]); }); it("resets domains independently", () => { const state = parseWorkspaceState({ activities: [{ id: "1", entityId: "x", entityType: "prompt", kind: "opened", title: "x", route: "/", timestamp: "now" }], notifications: [{ id: "n", type: "information", title: {}, message: {}, createdAt: "now", read: false }] }); const reset = resetWorkspaceDomain("activity", state); expect(reset.activities).toEqual([]); expect(reset.notifications).toHaveLength(1); }); it("measures only Academy storage domains", () => { const values = new Map([["shabi-data", "123"], ["other", "secret"]]); const storage = { length: 2, key: (index: number) => [...values.keys()][index] ?? null, getItem: (key: string) => values.get(key) ?? null }; const result = measureWorkspaceStorage(storage); expect(result.domains).toHaveLength(1); expect(result.totalBytes).toBeGreaterThan(0); expect(WORKSPACE_MAX_BYTES).toBeGreaterThan(1_000_000); }); });
