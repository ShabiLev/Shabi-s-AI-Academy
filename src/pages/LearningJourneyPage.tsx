import { Link } from "react-router-dom"; import { courseLessons, courseModules } from "../course/courseData"; import { useLanguage } from "../i18n/LanguageContext";
export function LearningJourneyPage() { const { language } = useLanguage(); return <div className="page"><header className="page-heading"><h1>{language === "he" ? "מסע הלמידה" : "Learning Journey"}</h1><p>{language === "he" ? "כל השיעורים פתוחים; הסדר הוא המלצה בלבד." : "Every lesson is open; the sequence is a recommendation only."}</p></header><ol className="runtime-timeline">{courseModules.map((module) => <li key={module.id}><span>{module.order}</span><div><h2>{module.title[language]}</h2><p>{module.description[language]}</p><ul>{courseLessons.filter((lesson) => lesson.moduleId === module.id).map((lesson) => <li key={lesson.id}><Link to={`/lessons/${lesson.slug}`}>{lesson.title[language]}</Link></li>)}</ul></div></li>)}</ol></div>; }
